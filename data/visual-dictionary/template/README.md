# 엑셀 협업 템플릿

1. `visual_dictionary_template.csv` 를 **Excel**에서 엽니다. (한글이 깨지면 Excel → 데이터 → 텍스트/CSV → UTF-8)
2. 새 단어 행을 추가합니다. `file_name` 은 `public/visual-dictionary/` 규칙과 동일하게 짓습니다.
3. **다른 이름으로 저장 → CSV UTF-8** 후 PicBook 편집기에서 **CSV 가져오기** 합니다.

예시 작품 전체 목록: `../stories/tortoise_and_hare.csv`
